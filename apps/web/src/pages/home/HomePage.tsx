import { AppShell } from '../../shared/ui/AppShell'

/** 홈 페이지 화면 또는 UI 요소를 접근 가능한 형태로 렌더링한다. */
export function HomePage() {
  return (
    <AppShell>
      <p className="text-ink-subtle text-sm">Talk후감 프론트엔드 기반을 준비하고 있어요.</p>
    </AppShell>
  )
}
