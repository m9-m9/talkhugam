# Talk후감 디자인 방향

이 문서는 Talk후감의 전체 화면을 더 담백한 블랙앤화이트 기반 UI로 정리하기 위한 시각 기준이다. 기존 Warm Coral 브랜드를 폐기하지 않고, 넓은 배경색이 아니라 필요한 순간의 작은 신호색으로 줄인다.

## 핵심 방향

Talk후감은 “책을 핑계로 이어지는 친밀한 대화”가 먼저 보여야 한다. 화면은 독서 기록 앱처럼 꾸미기보다, 카카오톡에 익숙한 사용자가 바로 이해할 수 있는 대화형 구조를 더 정제된 흑백 톤으로 보여 준다.

- 기본 인상은 블랙, 화이트, 회색으로 만든다.
- 코랄은 브랜드 존재감을 알리는 보조 신호로만 쓴다.
- 둥근 박스와 카드 장식을 줄이고, 여백·선·텍스트 무게로 위계를 만든다.
- 아이콘은 선형 아이콘을 기본으로 하며, 장식보다 명령의 의미가 먼저 보여야 한다.
- 책 표지, 프로필 이미지, 영상 썸네일 같은 사용자 콘텐츠만 화면의 색감을 만든다.

## 색상 원칙

### 기본 팔레트

| 역할 | 색상 | 용도 |
| --- | --- | --- |
| Ink | `#111111` | 주요 텍스트, 내 말풍선, 핵심 아이콘 |
| Ink Soft | `#3A3A3A` | 보조 제목, 강조가 낮은 텍스트 |
| Gray | `#767676` | 메타 정보, 시간, 설명문 |
| Border | `#E5E5E5` | 구획선, 입력창, 약한 경계 |
| Surface | `#FFFFFF` | 기본 화면과 카드 표면 |
| Surface Soft | `#FAFAFA` | 아주 약한 배경 분리 |
| Coral Signal | `#FF6F4D` | 활성 반응, 보내기, 중요한 브랜드 포인트 |

### 사용 비율

- 흑백과 회색이 화면의 90% 이상을 차지한다.
- 코랄은 한 화면에서 1-2개 핵심 상태에만 보인다.
- 크림, 살구, 베이지 계열 배경을 넓게 깔지 않는다.
- `bg-primary/10`, `bg-surface-muted`, 코랄 border tint는 반복 사용하지 않는다.
- 위험 상태는 코랄과 구분되는 명확한 error token을 사용한다.

### 코랄을 써도 되는 곳

- 보내기 버튼의 활성 상태
- 사용자가 선택한 이모지 반응
- 현재 탭이나 현재 단계의 작은 underline 또는 dot
- 브랜드 로고나 온보딩의 제한된 포인트

### 코랄을 피해야 하는 곳

- 페이지 전체 배경
- 큰 카드 배경
- 보조 버튼 전체 면
- 일반 안내문, 서브타이틀, 카테고리 라벨
- 반복 목록의 모든 활성/hover 배경

## 형태와 표면

둥근 박스가 많아지면 화면이 조립식으로 보인다. Talk후감은 카드 UI보다 대화와 콘텐츠 흐름이 먼저다.

- 페이지 섹션을 카드로 감싸지 않는다.
- 반복 아이템, 입력창, 말풍선, dialog처럼 필요한 곳에만 표면을 둔다.
- border는 `1px` 기본이며, 여러 겹의 border를 중첩하지 않는다.
- shadow는 floating layer, dialog, popover에만 약하게 사용한다.
- 한 화면에 큰 rounded rectangle이 4개 이상 보이면 구조를 다시 점검한다.
- radius는 작게 쓴다. 기본은 `8px`, 말풍선과 modal은 필요할 때만 `12px-16px`까지 허용한다.
- pill 형태는 badge, reaction count, compact tab처럼 짧은 정보에만 쓴다.

## 타이포그래피

한글 UI는 굵기 남발이 촌스러움으로 바로 이어진다. 글자 크기보다 weight와 줄 간격을 먼저 정리한다.

- `font-bold`는 화면당 1-2개 핵심 제목에만 쓴다.
- 버튼, 카드 제목, 말풍선 이름은 `font-medium` 또는 `font-semibold`를 기본으로 한다.
- 보조 설명은 색과 크기로 낮추고, 굵기로 강조하지 않는다.
- 긴 책 제목은 본문 대형 제목으로 반복하지 말고 header나 compact label에 담는다.
- 320px에서 한 글자나 조사만 다음 줄에 남으면 문구를 줄인다.

## 아이콘

아이콘은 Talk후감의 새 분위기를 만드는 중요한 기준이다. 기본은 얇고 명확한 선형 아이콘이다.

- 가능한 경우 `lucide-react` 또는 기존 선형 아이콘 계열을 사용한다.
- 아이콘 버튼은 최소 44px 터치 영역을 가진다.
- 아이콘은 18-22px를 기본으로 하고, 주요 action만 24px까지 허용한다.
- filled icon은 선택 상태, 재생, 강한 CTA처럼 의미가 뚜렷할 때만 쓴다.
- 텍스트 설명이 필요한 아이콘은 `aria-label`과 hover/focus tooltip 또는 인접 label을 제공한다.

## 레이아웃

기존 Talk후감의 320px 최소 폭과 데스크톱 최대 640px 중앙 캔버스 규칙은 유지한다.

- 페이지 기본 좌우 여백은 16px이다.
- 24px 이상 여백은 320px에서 콘텐츠 폭을 해치지 않을 때만 쓴다.
- 세로 간격은 Foundation scale `0/4/8/12/16/24/32/40/48/64`만 쓴다.
- dense screen에서는 장식 여백보다 정보 간격을 우선한다.
- fixed bottom navigation이나 composer가 있는 화면은 마지막 콘텐츠가 가려지지 않아야 한다.
- popover, emoji picker, action menu는 320px와 640px 모두 화면 밖으로 나가지 않아야 한다.

## 대화 화면 규칙

책 대화 화면은 새 디자인 방향의 기준 화면이다.

- 내 메시지는 black bubble, 상대 메시지는 white 또는 very light bubble을 기본으로 한다.
- 말풍선은 내용 너비에 맞추되, 너무 좁은 답글은 최소 너비를 둔다.
- 답글은 원문 바로 아래에 한 단계만 들여쓰기하거나 얇은 연결선으로 표현한다.
- reaction count는 말풍선 아래에 작게 두고, 큰 배지를 만들지 않는다.
- 답글/반응 action은 hover, focus, long press 등 필요한 순간에만 보인다.
- 입력창은 큰 카드가 아니라 하단 composer bar로 보이게 한다.
- 이모지 picker는 floating layer이며 화면 경계 안에서 위/아래 배치를 자동 조정한다.

## 책갈피와 영상 화면 규칙

책갈피는 기록 카드보다 “특별한 구절에 꽂은 표시”처럼 보여야 한다.

- 책갈피 목록은 큰 카드 반복보다 얇은 divider와 콘텐츠 썸네일 중심으로 정리한다.
- 영상은 카드 안에서 과하게 커지지 않고 제자리에서 재생된다.
- CTA는 촬영/업로드처럼 명확한 행동을 말하되, 두 버튼 간격은 최소 12px 이상 유지한다.
- 빈 상태는 한 문장과 한 행동만 남긴다.

## SEED 컴포넌트 사용

SEED는 접근성과 상호작용의 기반으로 사용하되, 기본 시각 톤을 그대로 가져와 화면을 중립 SaaS처럼 만들지 않는다.

- ActionButton, Dialog, Sheet, Field, Tabs, Switch, Checkbox는 우선 검토한다.
- SEED 컴포넌트의 바깥 여백은 상위 wrapper에서 관리한다.
- recipe CSS가 margin을 초기화할 수 있으므로 간격은 wrapper의 `gap`, `mt-*`로 보장한다.
- SEED variant가 코랄 면을 과하게 만들면 Talk후감 wrapper class로 흑백 톤을 조정한다.

## 페이지별 점검 체크리스트

각 페이지를 수정할 때 아래 순서로 본다.

1. 이 화면에서 사용자가 해야 할 다음 행동이 3초 안에 보이는가?
2. 코랄이 화면 면적의 10%를 넘지 않는가?
3. 큰 rounded rectangle이 4개 이상 보이지 않는가?
4. card 안에 card가 들어가지 않았는가?
5. `font-bold`가 반복되어 모든 요소가 외치고 있지 않은가?
6. 아이콘 버튼의 의미와 터치 영역이 충분한가?
7. 320px에서 텍스트가 잘리고 화면이 좌우로 넘치지 않는가?
8. 640px 중앙 캔버스에서도 콘텐츠가 과하게 넓거나 비어 보이지 않는가?
9. loading, empty, error, retry 상태가 같은 톤으로 정리됐는가?
10. Playwright screenshot에서 색이 아니라 구조만으로도 위계가 읽히는가?

## 마이그레이션 순서

모든 페이지를 한 번에 바꾸기보다, 사용자가 가장 자주 보는 흐름부터 새 기준을 확정한다.

1. 책 대화 화면: header, tab, message bubble, reply, reaction, composer
2. 책갈피 탭과 영상 inline playback
3. 책방 목록과 책방 상세
4. 책 검색, 책 대화 관리, 완독 기록
5. 프로필, 설정, 알림, 초대
6. 로그인, 온보딩, 법적 문서

각 단계는 320px와 640px 시각 검수, `pnpm test:web`, `pnpm build:web`, `pnpm --filter @talkhugam/web test:e2e` 통과 후 다음 단계로 이동한다.

## 금지 패턴

- 넓은 크림/살구 배경으로 따뜻함을 표현하기
- 코랄 tint 카드와 코랄 border를 반복하기
- 모든 기능을 rounded card로 감싸기
- 버튼 두 개를 붙여 배치하기
- 보조 설명까지 bold 처리하기
- 아이콘 없는 텍스트 버튼을 좁은 도구 UI에 반복하기
- popover가 320px 또는 640px 화면 밖으로 나가기
- 화면마다 다른 radius, 다른 shadow, 다른 CTA 색을 쓰기

## 최종 판단 기준

좋은 Talk후감 화면은 색을 빼도 Talk후감처럼 보여야 한다. 책 표지와 대화 내용이 색을 만들고, UI는 조용히 길을 열어 준다. 따뜻함은 코랄 배경이 아니라 문장, 대화 간격, 반응의 타이밍, 읽기 쉬운 구조에서 나온다.
